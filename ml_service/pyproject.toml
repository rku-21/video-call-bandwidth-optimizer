[project]
name = "ml_service"
version = "0.1.0"
description = "Adaptive calling ML service (WebRTC encoding recommendation)"
requires-python = ">=3.10"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
